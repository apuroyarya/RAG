# Ingestion backbone
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
psycopg[binary]>=3.2
pymupdf>=1.24
pillow>=10.0

# Embeddings - install separately to control the torch variant:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install sentence-transformers
# EMBED_BACKEND=fake needs neither.
